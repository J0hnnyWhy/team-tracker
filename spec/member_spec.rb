require('rspec')
require "pry"
require('member')
require "capybara/rspec"
require "./app"
Capybara.app = Sinatra::Application

describe('String') do
  before() do
    .clear()
  end

  describe('#') do
  it("") do
    expect(("the the").xxxxxxxx()).to(eq("the" "the"))
  end
end
end
